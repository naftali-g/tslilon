# Generated / derived — rebuilt by tools/merge_lexicon.py + build_lexicon.py
/tools/words.py
/lexicon.json
/tools/coverage.md

# Niqqud audit — generated review page + cached API responses (the corrections in
# niqqud_accepted.json and the source edits ARE tracked; these two are not)
/tools/niqqud_review.html
/tools/.niqqud_cache.json
/tools/niqqud_decisions*.json

# Python / OS / editor junk
__pycache__/
*.pyc
.DS_Store

# Local tooling config
.claude/settings.local.json

# Logo working files (logo.png is the tracked master)
/logo_clean.png
/logo_original.png

# Root HTML is dev/generated — track only the built index.html
# (tools/template.html is in a subdir, so it stays tracked)
/*.html
!/index.html
